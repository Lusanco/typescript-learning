{
  "scripts": {
    "db": "json-server -w db.json",
    "parcel": "npx parcel index.html",
    "dev": "concurrently npm:db npm:parcel"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "concurrently": "^9.1.0",
    "json-server": "^0.17.4",
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "process": "^0.11.10"
  }
}
